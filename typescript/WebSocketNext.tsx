import React, { FC } from 'react'
import { WebSocketProps } from './types/WebSocket.props'

const WebSocketNext: FC<WebSocketProps> = (props) => {
  return <div {...props} />
}

export default WebSocketNext
